import React from 'react'

type UserProfileProps = {
    name:string,
    age:number,
    address:string
}

const User = ({name,age,address}:UserProfileProps) => {
  return (
    <div>

    </div>
  )
}

